spring:
  flyway:
    locations:
      - classpath:db/migration
      - classpath:db/devseed

management:
  endpoints:
    web:
      exposure:
        include: health,info,mappings

logging:
  level:
    org.springframework.web: DEBUG
    org.springframework.security: INFO
    com.repnexa: DEBUG
